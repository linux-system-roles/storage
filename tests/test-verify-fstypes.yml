---
- name: Debug
  debug:
    msg: "hi"
# - name: Loop through storage volumes to check for fs-type and mount info
#   assert:
#     that:
#       - ansible_mounts|selectattr('mount', 'equalto', storage_pool.mount_point)|list|length==1
#       - ansible_mounts|selectattr('fsype', 'equalto', storage_pool.fs_type)|list|length==1
#     msg: "The state of the storage volume {{ storage_volume.name }} does not match the an entry in ansible_mounts"
#   when: storage_volume.state in 'present'
#   loop: "{{ pools_list }}"
#   loop_control:
#     loop_var: storage_pool
#
# - name: Loop through storage volumes to check for fs-type and mount info
#   assert:
#     that:
#       - ansible_mounts|selectattr('mount', 'equalto', storage_volume.mount_point)|list|length==1
#       - ansible_mounts|selectattr('fsype', 'equalto', storage_volume.fs_type)|list|length==1
#     msg: "The state of the storage volume {{ storage_volume.name }} does not match the an entry in ansible_mounts"
#   when: storage_volume.state in 'present'
#   loop: "{{ volumes_list }}"
#   loop_control:
#     loop_var: storage_volume
