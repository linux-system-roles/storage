---
- name: Make sure all volumes in pool.volumes are absent
  assert:
    that: current_volume.mount_point not in fstab_buffer.stdout
    msg: "The volume {{ current_volume.name }} was found in /etc/fstab file while run in absent state"
  loop: "{{ current_pool.volumes }}"
  loop_control:
    loop_var: current_volume
