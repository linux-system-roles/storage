---
language: python
services:
  - docker
install:
  - pip install molecule docker tox
script:
  - test -f tox.ini && tox
  - molecule --version
  - ansible --version
  - molecule lint
  - molecule syntax
  - molecule test
