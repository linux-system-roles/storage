---
#
# Set the list of PV devices.
#
# TODO: Decide on semantics. Playbook PV specs should probably be complete to enable idempotency.
#
- name: Install LVM2 commmands as needed
  package:
    name: lvm2
    state: present
  when: pool.type == "lvm"
  
- block:
    - name: collect list of current pvs
      command: pvs -o name --noheadings --select 'vg_name={{ pool.name }}'
      register: pvs_cmd
      changed_when: false
      become: true
  rescue:
    - debug:
        msg: "Failed to run pvs"
  when: disks == [] and pool.type == "lvm" and pool.name in ansible_facts.lvm.vgs

- name: Set pvs based on disk set
  set_fact:
    pvs: "{{ disks }}"
  when: pool.type == "lvm"

- name: Set pvs from current vg
  set_fact:
    pvs: "{{ pvs_cmd.stdout.split() }}"
  when: disks == [] and pool.type == "lvm" and pool.name in ansible_facts.lvm.vgs

#
# Configure the VG
#
- block:
    - name: configure vg
      lvg:
        vg: "{{ pool.name }}"
        pvs: "{{ pvs }}"
        state: "{{ pool.state }}"
  rescue:
    - debug:
        msg: "Failed to configure vg"
  when: pool.type == "lvm" and pool.name

- block:
   - name: wipe pvs
     command: pvremove {{ pv_path }}
     changed_when: false
     with_items: "{{ pvs }}"
     loop_control:
       loop_var: pv_path
  rescue:
    - debug:
        msg: "Failed to wipe pv signatures."
  when: pool.state == "absent"
